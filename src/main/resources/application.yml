### UNCOMMENT spring.profiles.active for production/deployment build ###
### COMMENT-OUT spring.profiles.active for development/testing ###
##--------------------------------------------------------------------##
#spring:
#  profiles:
#    active: @spring.profiles.active@

server:
  port: 8081
#  servlet:
#    context-path: /api


logging:
  level:
    web: DEBUG


app:
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5433/amigoscodedemo
    username: postgres
    password: password
    pool-size: 30     # Allowed number of given connections at any given time.

